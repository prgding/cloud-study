server:
  port: 8080
spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
    gateway:
      routes:
        - id: oneApp
          uri: lb://oneApp
          predicates:
            - Path=/one
  main:
    web-application-type: reactive